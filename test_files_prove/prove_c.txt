proc main():
    var a, b , c , d , e , f , g, h, j : integer;\
    var u : real;\

    %a, b , c, d ^= bau(e, u), bau(e,u);%

    e, f ^= miao(e,e), 5+d ;

    muu(e, @f);

endproc

func miao(x:integer, y:integer)-> integer:
    return 5;
endfunc

func bau(belzebu : integer, satan : real) -> integer, integer:
    %return 7;%
    return 77, 66;
endfunc

proc muu(x:integer, out y:integer):
    y ^= 5;
    x ^= 3;

    muu(x, @y);

    %if y<3 then
        y, x ^=3, y;

    elseif y<>7 then
        x^=y;
    else
        y^=0;
    endif;

    while y>3 do
      y ^= 6;
    endwhile;

    --> "ciao" + "miao" $(x) $(y);

    <-- "ciao" + "bau" $(x) "miao" $(y);%

endproc